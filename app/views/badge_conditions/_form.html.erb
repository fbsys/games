<%= form_for(@badge_condition) do |f| %>
  <% if @badge_condition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@badge_condition.errors.count, "error") %> prohibited this badge_condition from being saved:</h2>

      <ul>
      <% @badge_condition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
