<% @comments.each do |comment| %>
      <%= comment.created_at.strftime('%Y-%m-%d %H:%M') %>   [<%= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' }, remote: true %>]
	  <p class="triangle-border left"><%= comment.body %></p>
<% end %>
