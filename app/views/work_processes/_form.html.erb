<%= form_for(@work_process) do |f| %>
  <% if @work_process.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_process.errors.count, "error") %> prohibited this work_process from being saved:</h2>

      <ul>
      <% @work_process.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :process_code %><br>
    <%= f.text_field :process_code %>
  </div>
  <div class="field">
    <%= f.label :status_type %><br>
    <%= f.text_field :status_type %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
