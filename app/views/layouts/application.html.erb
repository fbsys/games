<!DOCTYPE html>
<html>
<head>
  <title>GALAX</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Home</a>
      </div>
      <div class="collapse navbar-collapse">
        <p class="navbar-text pull-right">
          <% if user_signed_in? %>
            Logged in as <strong><%= current_user.email[0,5] %></strong>.
            <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
            <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
            <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
          <% end %>
        </p>
        <ul class="nav navbar-nav">
          <li class="<%= "active" if controller.controller_name == "avatars" %>"><%= link_to 'Avatars', avatars_path %></li>
          <li class="<%= "active" if controller.controller_name == "badge_logs" %>"><%= link_to 'Badges', badge_logs_path %></li>
          <li class="<%= "active" if controller.controller_name == "comments" %>"><%= link_to 'Comments', comments_path %></li>
          <li class="<%= "active" if controller.controller_name == "database" %>"><a><s>DB-Knowldege</s></a></li>
          <li class="<%= "active" if controller.controller_name == "event" %>"><a><s>Event</s></a></li>
          <li class="<%= "active" if controller.controller_name == "f????" %>"><a><s>F????</s></a></li>
          <li class="<%= "active" if controller.controller_name == "goals" %>"><a><s>Goal</s></a></li>
        </ul>
      </div>
    </div>
  </nav>
  <div class="yieldbody">
    <div class="container">
      <% if notice %>
        <p class="alert alert-notice"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-error"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
  </body>
  <footer>
    <div class="container">
      "Update the world." X 2014
    </div>
  </footer>
  <script src="//railsgirls.com/assets/bootstrap.js"></script>
  <link rel="stylesheet" href="//railsgirls.com/assets/bootstrap.css" />
  <link rel="stylesheet" href="//railsgirls.com/assets/bootstrap-theme.css" />
</body>
</html>
